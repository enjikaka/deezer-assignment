const Backbone = window._Backbone;

export const Router = Backbone.Router;
export const Model = Backbone.Model;
export const Collection = Backbone.Collection;
export const View = Backbone.View;

export default Backbone;
